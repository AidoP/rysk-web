{
    "tasks": {
        "wasm": "wasm-pack build --reference-types -d src/rysk_web/",
        "dev": "cargo watch -i src/rysk_web/ -- wasm-pack build --reference-types -d src/rysk_web/ & deno run -A npm:vite",
        "vue-dev": "deno run -A npm:vite",
        "build": "deno task wasm && deno run -A npm:vite build",
        "preview": "deno run -A npm:vite preview",
        "serve": "deno run --allow-net --allow-read https://deno.land/std@0.157.0/http/file_server.ts target/dist/",
        "check": "cargo test && deno task wasm && deno run --allow-read --allow-env --allow-sys vue-tsc.ts --noEmit",
        "lint": "cargo clippy -- -Dwarnings && deno lint src/ --ignore=src/rysk_web/"
    },
    "nodeModulesDir": true
}
